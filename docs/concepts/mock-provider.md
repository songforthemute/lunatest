# Mock Provider Principle

The Mock Provider focuses on state changes that frontend code can actually observe.

In Scope:

- chain ID, accounts, balances, and transaction state
- how the UI changes during approval and swap flows

Out of Scope:

- exact AMM math fidelity
- full EVM execution reproduction
