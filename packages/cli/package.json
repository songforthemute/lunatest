{
  "name": "@lunatest/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "lunatest": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "commander": "^14.0.3"
  }
}
